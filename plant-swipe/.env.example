# Frontend (Vite) environment
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: will infer DB host from SUPABASE_URL when present
SUPABASE_DB_PASSWORD=

# Database direct connection (if not using SUPABASE_* inference)
# DATABASE_URL=postgresql://user:pass@host:5432/database?sslmode=require

# Alternative PG vars (server tries to construct if DATABASE_URL absent)
# PGHOST=
# PGUSER=
# PGPASSWORD=
# PGDATABASE=postgres
# PGPORT=5432

# Server
PORT=3000
NODE_ENV=production

