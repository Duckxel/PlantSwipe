# Native icon / splash sources

- `logo.png` / `logo-dark.png` — fed to `@capacitor/assets` (see `bun run cap:assets`).
- Regenerate Android + iOS launcher and splash assets after changing these files.
