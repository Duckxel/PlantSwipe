# GDPR Compliance Audit — 2026-04-29

Auto-generated compliance review based on this week's merged PRs (2026-04-22 to 2026-04-29).

## Scope

**PRs reviewed:** 6
**Commits reviewed:** 13
**New data surfaces found:** 2

### PRs Included

| PR | Title | GDPR Relevance |
|----|-------|----------------|
| [#1448](https://github.com/Duckxel/PlantSwipe/pull/1448) | Add plant change history and admin notes thread | **HIGH** — 2 new database tables storing user-authored content |
| [#1449](https://github.com/Duckxel/PlantSwipe/pull/1449) | Remove unused gp variable from OverviewSection | None — code cleanup |
| [#1447](https://github.com/Duckxel/PlantSwipe/pull/1447) | Add automatic pruning of old GitHub releases | None — CI/CD only |
| [#1445](https://github.com/Duckxel/PlantSwipe/pull/1445) | Upgrade Node.js to 24.x | None — infrastructure only |
| [#1446](https://github.com/Duckxel/PlantSwipe/pull/1446) | Fix nginx reload during DNS hiccup | None — ops fix only |
| [#1444](https://github.com/Duckxel/PlantSwipe/pull/1444) | Object.values optimization | None — performance only |

---

## New Data Surfaces

### 1. `plant_history` table (PR #1448)

- **What data is stored:** `id` (uuid), `plant_id` (text), `author_id` (uuid), `action` (text), `field` (text), `summary` (text), `created_at` (timestamptz)
- **Where it is stored:** Supabase PostgreSQL — `public.plant_history`
- **User linkage:** `author_id` references `public.profiles(id)` with `ON DELETE SET NULL`
- **Nature:** Insert-only audit log recording admin actions on plant records (field edits, translations, AI fills, note mutations, status changes). The `summary` field contains human-readable change descriptions (capped at 180 chars) which may include field values.

### 2. `plant_admin_notes` table (PR #1448)

- **What data is stored:** `id` (uuid), `plant_id` (text), `author_id` (uuid), `body` (text), `created_at` (timestamptz), `updated_at` (timestamptz)
- **Where it is stored:** Supabase PostgreSQL — `public.plant_admin_notes`
- **User linkage:** `author_id` references `public.profiles(id)` with `ON DELETE SET NULL`
- **Nature:** Chat-style editorial notes per plant. Admins can create, edit, and delete notes. `body` is free-text user-generated content.

---

## Export Coverage (Right to Data Portability — GDPR Article 20)

**Export handler location:** `plant-swipe/server.js` — `GET /api/account/export` (line 19303)

### No Issues Found

All previously covered data surfaces remain in the export handler. No regressions detected.

### Export Gaps

- **`plant_history`** (added in PR #1448) — **FIXED**
  - The export handler now queries `plant_history` by `author_id` and surfaces the rows under the `plantHistoryAuthored` key.
  - **Implemented in:** `plant-swipe/server.js`, item 29 inside `gdprParallelResults`, with `plantHistoryAuthored` added to `gdprKeys` / `gdprLabels`.

- **`plant_admin_notes`** (added in PR #1448) — **FIXED**
  - The export handler now queries `plant_admin_notes` by `author_id` and surfaces the rows under the `plantAdminNotesAuthored` key.
  - **Implemented in:** `plant-swipe/server.js`, item 30 inside `gdprParallelResults`, with `plantAdminNotesAuthored` added to `gdprKeys` / `gdprLabels`.

---

## Deletion Coverage (Right to Erasure — GDPR Article 17)

**Deletion handler location:** `plant-swipe/server.js` — `POST /api/account/delete-gdpr` (line 18642)

### Partial Coverage via FK Cascade

Both new tables define their `author_id` foreign key with `ON DELETE SET NULL` referencing `public.profiles(id)`. Since the GDPR deletion handler explicitly deletes the user's profile row (step 15, ~line 19136: `DELETE FROM public.profiles WHERE id = $userId`), the FK cascade sets `author_id = NULL` on all matching rows in both tables. This **anonymizes the authorship link**.

### Deletion Gaps

- **`plant_admin_notes.body`** — **FIXED (option B — anonymize)**
  - The deletion handler now performs `UPDATE public.plant_admin_notes SET body = '[deleted]', author_id = NULL WHERE author_id = $userId` before profile deletion (step 14p), preserving thread structure for other admins while removing free-text content. Counter `plantAdminNotesAnonymized` added to `stats`.
  - **Rationale:** Anonymization preserves operational/editorial context that other admins rely on, and matches the existing pattern used for `admin_media_uploads` (step 14n). Both options satisfy GDPR; option B was chosen for consistency with the codebase.
  - **Implemented in:** `plant-swipe/server.js`, step 14p in the `POST /api/account/delete-gdpr` handler.

- **`plant_history.summary`** — **CONFIRMED no PII; counter added**
  - Code review of `plant-swipe/src/lib/plantHistoryDiff.ts:205` confirms summaries are constructed as `Changed ${label}` where `label` is a static field-name from `FIELD_LABELS`. No user values, display names, or emails are interpolated into summaries. Other call sites (`plantAdminNotes.ts`, `aiPrefillService.ts`) use static strings or plant names only.
  - The FK `ON DELETE SET NULL` cascade is therefore sufficient for plant_history. A counter (`plantHistoryAnonymized`) was added to `stats` for audit-log completeness (step 14o) — it counts rows where `author_id = userId` immediately before the cascade fires.
  - **Implemented in:** `plant-swipe/server.js`, step 14o in the `POST /api/account/delete-gdpr` handler.

- **Stats coverage** — **FIXED**
  - The `stats` object now includes `plantHistoryAnonymized` and `plantAdminNotesAnonymized` so the GDPR audit log (`logGdprAccess`) records that these tables were affected during deletion.

---

## Third-Party Services

No new third-party service integrations were introduced this week. All changes in scope are internal (database schema, CI/CD, nginx configuration, frontend optimization).

---

## Resolved (was flagged for human review)

1. **`plant_admin_notes` deletion strategy** — Resolved in this commit by choosing **option B (anonymize)**: `body` is replaced with `'[deleted]'` and `author_id` is nulled. This satisfies GDPR while preserving thread structure for other admins and matches the existing anonymization pattern used for `admin_media_uploads`. If the team prefers full deletion, swap step 14p for a `DELETE FROM public.plant_admin_notes WHERE author_id = $userId`.

2. **`plant_history.summary` field** — Resolved by code review of `plant-swipe/src/lib/plantHistoryDiff.ts`. The `summary` is built as `Changed ${label}` where `label` is a static field name from `FIELD_LABELS`; no user values are interpolated. Other producers (`plantAdminNotes.ts`, `aiPrefillService.ts`) use static strings or plant names only. The existing `ON DELETE SET NULL` cascade is sufficient.

3. **Deletion handler stat tracking** — Resolved by adding `plantHistoryAnonymized` and `plantAdminNotesAnonymized` to the `stats` object.

---

## Action Required

| Priority | Table | Obligation | Status | Action |
|----------|-------|-----------|--------|--------|
| **High** | `plant_history` | Export | Resolved | Added to export handler (`plantHistoryAuthored`) |
| **High** | `plant_admin_notes` | Export | Resolved | Added to export handler (`plantAdminNotesAuthored`) |
| **Medium** | `plant_admin_notes` | Deletion | Resolved | Anonymized (`body = '[deleted]'`, `author_id = NULL`) before profile deletion |
| **Low** | `plant_history` | Deletion | Resolved | Confirmed no PII in `summary`; counter `plantHistoryAnonymized` added |

---

## No Issues Found (Existing Coverage)

The following data surfaces were already covered in both export and deletion handlers prior to this week's changes. No regressions detected:

- `profiles` — export + deletion
- `messages` / `conversations` — export + deletion
- `friends` / `friend_requests` — export + deletion
- `gardens` / `garden_members` / `garden_plants` — export + deletion
- `garden_journal_entries` / `garden_journal_photos` — export + deletion (including storage)
- `plant_scans` — export + deletion (including storage)
- `bug_reports` — export + deletion (including screenshots)
- `bookmarks` — export + deletion
- `user_notifications` — export + deletion
- `user_push_subscriptions` — export + deletion
- `user_badges` — export + deletion
- `event_registrations` / `event_user_progress` — export + deletion
- `user_action_status` — export + deletion
- `garden_activity_logs` — export + deletion
- `garden_user_activity` — export + deletion
- `garden_task_user_completions` — export + deletion
- `requested_plants` — export + deletion
- `bug_action_responses` / `bug_points_history` — export + deletion
- `message_reactions` — export + deletion
- `discovery_seen_plants` — export + deletion
- `garden_plant_images` — export + deletion (including storage)
- `admin_media_uploads` — export + anonymization on deletion
- `web_visits` — anonymized on deletion
- `user_cookie_consent` — export
- `garden_invites` — export + deletion
- `user_blocks` — deletion
- `garden_roadmap_completions` — export + deletion
