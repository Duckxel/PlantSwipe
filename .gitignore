# dependencies
node_modules/

# Package manager lock files (bun is primary)
# Ignore npm's lock file since we use bun:
package-lock.json
# bun.lock
# bun.lockb

# builds & bundles
dist/
build/

# Vite cache
.vite/

# TypeScript
*.tsbuildinfo

# logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
lerna-debug.log*
*.log

# env files (keep an example)
.env
.env.*
!.env.example

# test coverage
coverage/
*.lcov

# OS/editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/

# misc caches
.cache/
tmp/
temp/

# deployment tracking
TIME

# hosting/platform folders (if you use them)
.vercel/
.netlify/

# Mobile signing / distribution (never commit; use CI secrets)
*.jks
*.keystore
*.p12
*.mobileprovision
*.cer
artifacts/
release/
captures/

# Supabase CLI artifacts
/supabase/
plant-swipe/supabase/.temp/
plant-swipe/supabase/config.toml

# Domain configuration (server-specific)
domain.json

# Embedded Aphydle daily-game repo (cloned by setup.sh into ./aphydle/).
# It has its own .git/, .env, dist/, node_modules — never commit any of it
# back into PlantSwipe.
/aphydle/

# Bun runtime
.bun/
*.bun

# Admin API environment file (created locally, contains secrets)
admin_env

# Core dump files (crash artifacts, not needed)
core
core.*
*.core

# Editor settings that may auto-generate
*.sublime-workspace
*.sublime-project

# Debug
*.pid
*.seed
*.pid.lock

# Yarn (if accidentally used)
yarn.lock
.yarn/
.yarnrc.yml
.pnp.*
admin_api/__pycache__/
admin_api/venv/
admin_api/tests/__pycache__/
